public enum ParticleType
{
    Proton, Neutron, Electron
}

public enum MotionType
{
    Straight, Point, ShrinkKill
}

public enum Throwable
{
    Proton, Neutron, Electron, GammaRay
}

public enum ChosenChoicePanel
{
    Cluster, Other
}

public enum NuclideChangeStates
{
    Nothing, Fusion, Division, AcceptInner
}

public enum UIWindow
{
    EscapeMenu, NuclideCard, CustomWindow, None
}